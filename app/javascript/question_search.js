class QuestionSearch {
  constructor(options){
    this.searchbox = $(options.searchBox);
    this.question
  }
}

$(document).ready(()=>{
  var options = {
    searchBox = 'input[data-id="question-searchbox"]'
  };
});
