<%= javascript_include_tag "https://js.stripe.com/v3/" %>

<%= form_with  html: { class: 'payment_form' } do |f| %>
  <% if @registration.errors.any? %>
    <h3>
        <%= pluralize(@registration.errors.count, "error") %> prohibited this registration from being saved:
        <ul>
        <% @registration.errors.full_messages.each do |message| %>
          <li><%= message %>
        <% end %>
    </h3>
  <% end %>
  <%= f.hidden_field :course_id, value: @course.id %>
  <%= f.label :full_name %>
  <%= f.text_field :full_name %>
  <%= f.label :telephone %>
  <%= f.text_field :telephone %>
  <%# <%= script.stripe-button{ src: "https://checkout.stripe.com/checkout.js", %>
        <%# data: {amount: @course.price*100, description: @course.name, key: Rails.application.secrets.stripe_publishable_key}} %> %>
<% end %>
